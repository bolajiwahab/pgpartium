#!/bin/bash

usage() {
    echo "
    ${0##*/}

    Creates or updates a pull request on github.

    OPTIONS:
      -b  the branch name to use (Default: add-expire-partitions)
      -t  the title of the pull request (Default: Add/Expire partitions)
      -m  the commit message (Default: Add/Expire partitions)
      -h  show this help message.

    SAMPLE USAGE:
        ${0##*/} -b generate-partitions
"
}

while getopts "b:t:m:h" option; do
    case "${option}" in
    b)
        branch="${OPTARG}"
        ;;
    t)
        title="${OPTARG}"
        ;;
    m)
        commit_msg="${OPTARG}"
        ;;
    h | *)
        usage
        exit 0
        ;;
    esac
done

set -euo pipefail

branch=${branch:-"add-expire-partitions"}
title=${title:-"Add/Expire partitions"}
commit_msg=${commit_msg:-"${title}"}

if git diff --quiet -- && [ -z "$(git ls-files --others --exclude-standard)" ]; then
    echo "INFO: No changes detected. Exiting..."
    exit 0
fi

full_branch="pgpartium/${branch}"
base_branch=$(git remote show origin | grep 'HEAD branch' | awk '{print $NF}')

# Set up git config
git config --global user.email "github-actions[bot]@users.noreply.github.com"
git config --global user.name "github-actions[bot]"

# Fetch latest base branch from remote
git fetch origin "${base_branch}"

# Create or reset our branch
git checkout -B "${full_branch}" "origin/${base_branch}"

git add -A
git commit -m "${commit_msg}"

# Push to remote
git push origin "${full_branch}" --force

PR_URL="$(gh pr list --head "${full_branch}" --state open --json url --jq .[].url)"
if [[ -n "${PR_URL}" ]]; then
    echo "INFO: PR already exists -> ${PR_URL}. Exiting..."
    exit 0
else
    echo "INFO: Creating PR..."
    gh pr create --base "${base_branch}" \
        --head "${full_branch}" \
        --title "${title}" \
        --body "This PR adds/expires partitions."
    echo "INFO: PR created -> ${PR_URL}"
fi
