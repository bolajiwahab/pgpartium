---
lifecycle:
  directory: "tests/fixtures/expire_partitions/config_directory"
  partition:
    retention: "1 month"
    idempotent: true
  tables:
    - schema: "test"
      name: "events"
      output_file_template: "events.sql"
