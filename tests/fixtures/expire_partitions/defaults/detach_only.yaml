---
lifecycle:
  directory: tests/fixtures/expire_partitions/defaults
  idempotent: true
  partition:
    retention:
      interval: 1 month
      detach_only: true
  tables:
    - schema: test
      name: notifications
