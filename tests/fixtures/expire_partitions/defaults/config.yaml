---
lifecycle:
  directory: "tests/fixtures/expire_partitions/defaults"
  idempotent: true
  partition:
    retention:
      interval: "1 month"
  tables:
    - schema: "test"
      name: "notifications"
