---
lifecycle:
  directory: tests/fixtures/expire_partitions/defaults
  partition:
    retention:
      interval: 1 month
      detach_first: true
      detach_concurrently: true
  tables:
    - schema: test
      name: notifications
