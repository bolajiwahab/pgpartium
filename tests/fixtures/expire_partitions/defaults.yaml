---
generates:
  - directory: "tests/migrations"
    retention: "1 day"
    tables:
      - table_schema: "public"
        table_name: "notifications"
        file_name_template: "{description}.result"
        description:
          expire: "defaults"
