---
generates:
  directory: "tests/migrations"
  migration-tool:
    name: "flyway"
    version: "11.8.0"
  retention: "1 day"
  tables:
    - table_schema: "public"
      table_name: "notifications"
      file_name_template: "{description}.result"
      description:
        expire: "defaults"
