---
generates:
  directory: "tests/fixtures"
  retention: "1 day"
  tables:
    - table_schema: "test"
      table_name: "notifications"
      file_name_template: "{description}.result"
      description:
        expire: "defaults"
