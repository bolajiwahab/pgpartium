---
generates:
  directory: "tests/fixtures/expire_partitions"
  retention: "1 month"
  tables:
    - table_schema: "test"
      table_name: "notifications"
      file_name_template: "{description}.result"
      description:
        expire: "defaults"
