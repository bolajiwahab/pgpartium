---
lifecycle:
  directory: tests/fixtures/make_partitions/defaults
  tables:
    - schema: test
      name: transactions
      partition:
        interval: 1 mon
        start_timestamp: 2025-03-01T00:00:00Z
        naming:
          template: "{parent_table_schema}__{parent_table_name}__YYYY_MM"
