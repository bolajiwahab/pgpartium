---
generates:
  directory: "tests/fixtures"
  name_template: "{table_schema}__{table_name}__YYYY_MM"
  interval: "1 mon"
  partition_tablespace: "pgpartium"
  tables:
    - table_schema: "test"
      table_name: "transactions"
      file_name_template: "{description}.result"
      description:
        make: "partition_tablespace"
