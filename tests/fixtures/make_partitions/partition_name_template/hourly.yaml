---
lifecycle:
  directory: tests/fixtures/make_partitions/partition_name_template
  tables:
    - schema: test
      name: transactions
      partition:
        naming:
          template: "{parent_table_schema}__{parent_table_name}__YYYY_MM_DD_HH24"
        interval: 1 hour
        start_timestamp: 2025-04-01T00:00:00+00:00
