---
lifecycle:
  directory: tests/fixtures/make_partitions/start_timestamp_with_existing_partitions
  tables:
    - schema: test
      name: transactions
      partition:
        naming:
          template: "{parent_table_schema}__{parent_table_name}__YYYY_MM_DD"
        interval: 1 day
        start_timestamp: NOW
