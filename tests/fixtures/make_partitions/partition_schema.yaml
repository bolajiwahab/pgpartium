---
generates:
  - directory: "tests"
    name_template: "{table_schema}__{table_name}__YYYY_MM"
    interval: "1 mon"
    partition_schema: "partitions"
    tables:
      - table_schema: "public"
        table_name: "transactions"
        file_name_template: "{description}.result"
        description:
          make: "partition_schema"
