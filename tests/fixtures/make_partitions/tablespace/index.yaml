---
lifecycle:
  directory: tests/fixtures/make_partitions/tablespace
  tables:
    - schema: test
      name: transactions
      partition:
        naming:
          template: "{parent_table_schema}__{parent_table_name}__YYYY_MM"
        interval: 1 mon
        start_timestamp: 2025-04-01T00:00:00+00:00
        index:
          tablespace: pgpartix_fast
      template:
        schema: test
        name: transactions_template
