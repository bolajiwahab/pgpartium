---
lifecycle:
  directory: tests/fixtures/make_partitions/interval
  tables:
    - schema: test
      name: transactions
      partition:
        naming:
          template: "{table_schema}__{table_name}__YYYY_MM_DD_HH24"
        interval: 1 hour
        start_timestamp: 2025-04-01T00:00:00+00:00
