---
lifecycle:
  directory: examples/quick-start/migrations
  timezone: Etc/UTC
  partition:
    naming:
    # template: "{table_schema}__{table_name}__YYYY_MM"
    # interval: 1 month
    # start_timestamp: NOW
    future: 2
    # retention:
    #   # interval:
    #   detach_first: true
  tables:
    - schema: public
      name: transactions
      idempotent: true
