name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    strategy:
      matrix:
        postgresql:
          - 16
          - 17

    name: 🐘 PostgreSQL ${{ matrix.postgresql }}
    runs-on: ubuntu-latest
    container: ghcr.io/bolajiwahab/pgpartium:0.0.2@sha256:f369c025eddaa985f5e8b7da154a36bfa8acd1c0c32d3cf8563ee9a34fbcdbd2
    steps:
      - name: Start PostgreSQL ${{ matrix.postgresql }}
        run: pg-start -v ${{ matrix.postgresql }}

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build and test extension on PostgreSQL ${{ matrix.postgresql }}
        run: generate-partitions -c config.yml
